/**
 * Retorna o preset do tema selecionado (gradiente, accent, botão).
 * Atualiza imediatamente quando as preferências são salvas no perfil (via ThemeAccentContext).
 */
export { useThemeAccent } from "@/context/theme-accent-context";
